import { identity } from 'ramda';

/**
 * Description
 *
 * @func
 * @category {{category}}
 *
 * @param {any} x Description
 * @return {any} Description
 *
 * @example
 *
 *        R_.{{camelCase name}}() // []
 *
 * @sig a -> a
 */
const {{camelCase name}} = identity;

export default {{camelCase name}};
