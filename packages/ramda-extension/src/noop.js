import alwaysNull from './alwaysNull';

/**
 * Alias for `alwaysNull`
 *
 * @func
 * @category Function
 *
 * @sig a -> Object
 *
 * @example
 *
 *        R_.noop() // null
 *
 */
const noop = alwaysNull;

export default noop;
