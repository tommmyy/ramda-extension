import { always } from 'ramda';
/**
 * Always returns null.
 * @func
 * @category Function
 *
 * @example
 *
 *		alwaysNull() // null
 *
 * @sig a -> Object
 */
const alwaysNull = always(null);
export default alwaysNull;
